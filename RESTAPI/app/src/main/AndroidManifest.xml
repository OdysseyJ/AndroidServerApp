<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.restapi">
    <!-- 인터넷 연결 허용하는 퍼미션 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".LandmarkRecognitionShow"></activity>

        <meta-data
            android:name="com.google.firebase.ml.version.DEPENDENCIES"
            android:value="face" />
        <meta-data
            android:name="com.google.firebase.ml.version.DEPENDENCIES2"
            android:value="ocr" />

        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home" />
        <activity
            android:name=".ContactDialog"
            android:theme="@android:style/Theme.Dialog" />
        <activity android:name=".MoleHolePopupActivity" />
        <activity
            android:name=".CustomDialog"
            android:theme="@android:style/Theme.Dialog">
        </activity>

        <activity android:name=".ContactClickDialog"
            android:theme="@android:style/Theme.Dialog"></activity>

        <activity
            android:name=".ImageClickDialog"
            android:theme="@android:style/Theme.Dialog"></activity>
        <activity android:name=".FaceRecognition" />
        <activity android:name=".LandmarkRecognition" />
        <activity android:name=".TextRecognition" />
        <activity android:name=".MoleHoleActivity" />
        <activity android:name=".OneToFiftyActivity" />
        <activity android:name=".PopupActivity" />
        <activity android:name=".NamePopupActivity" />


        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>